# Colab-Compatible Requirements
# Use this version when running in Google Colab to avoid dependency conflicts

# Core dependencies (Colab-compatible versions)
opencv-python-headless==4.8.1.78
numpy>=1.26.0,<2.0
pandas>=2.2.0,<3.0

# PDF Parsing (install with --no-deps to avoid conflicts)
pdfminer.six>=20221105
pdf2image>=1.16.0
pypdf>=3.0.0
pypdfium2>=4.0.0
pillow>=10.0.0
pi-heif>=0.10.0

# Unstructured and dependencies (manual installation)
# Install these AFTER unstructured with --no-deps:
layoutparser>=0.3.0
lxml>=4.9.0
emoji>=2.0.0
langdetect>=1.0.9
nltk>=3.8.0
tabulate>=0.9.0
python-magic-bin>=0.4.14
beautifulsoup4>=4.11.0

# Graph Database
neo4j>=5.0.0

# Vector Database & Embeddings
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Web Interface
streamlit>=1.28.0
